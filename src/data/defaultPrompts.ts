import type { SmartPrompt } from '../types'

export const DEFAULT_PROMPTS: SmartPrompt[] = [
  {
    id: 'handwritten-ledger',
    name: '单页账本整扫',
    emoji: 'abacus',
    description: '只处理这一种 31 行固定账本，一张完整页面。',
    prompt:
      '识别一张完整单页的固定格式手写账本。只处理这一种 31 日固定本子：先确认整页账本四角、表头、日期列和 1日至31日都在同一张图片里；如果页面被裁掉、只拍到局部、缺少表头或缺少日期行，不要输出看似完整的局部结果，必须在 auditNotes 写明 pageIncomplete/cuttingRisk 并降低 overallConfidence。第一阶段优先读取简单去红压缩图：黑/蓝笔是每日原始格子数据，红色手写计算式、红色下划线和红色合计是人工验算结果，不能当作普通格子原始金额重复入账。定位账本表格四边和日期行网格后，按固定列和 31 个日期行读取每个可见数字或 X，能判断时写 cellId，例如 r16-paper-2；不要只输出数字列表。如果某格为空但可能有墨迹或压线，写入不确定/审计说明。红字在审计阶段用于对比 AI 重算值：若红字合计与格子重算冲突，标记 redCalculationConflict，写明冲突列、差值和疑似错读日期格，并降低置信度；不得为了匹配红字倒推修改看不清的黑字格。请按日期/行号从上到下抽取原始数字、列倍率、计算后金额，保留 X、空白、不确定数字，并给出每个金额在图片中的相对框选位置和置信度。明显非金额的记号不要计入总额。',
  },
]

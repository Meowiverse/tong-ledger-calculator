import type { SmartPrompt } from '../types'

export const DEFAULT_PROMPTS: SmartPrompt[] = [
  {
    id: 'handwritten-ledger',
    name: '单页账本整扫',
    emoji: 'abacus',
    description: '只处理这一种 31 行固定账本，一张完整页面。',
    prompt:
      '识别一张完整单页的固定格式手写账本。只处理这一种本子：先确认整页账本四角、表头、日期列和 1日至31日都在同一张图片里；如果页面被裁掉、只拍到局部、缺少表头或缺少日期行，不要输出看似完整的局部结果，必须在 auditNotes 写明 pageIncomplete/cuttingRisk 并降低 overallConfidence。定位账本表格四边和日期行网格后，按固定列读取：日期、上班、纸类1、纸类2、纸类3、纸类4、上下货、扣款、日合计。第一行小数是纸类单价/倍率；0.1、0.088、0.05 等只作用于对应纸类列。每个可见数字或 X 必须归属到日期行和列，能判断时写 cellId，例如 r16-paper-2；不要只输出数字列表。如果某格为空但可能有墨迹或压线，写入不确定/审计说明。上下货按已算好的直接金额处理，扣款单独作为负向调整。请按日期/行号从上到下抽取原始数字、列倍率、计算后金额，保留 X、空白、不确定数字，并给出每个金额在图片中的相对框选位置和置信度。明显非金额的记号不要计入总额。',
  },
]
